# docker-spring-boot-accdb

### How to run 

* Clone the project.

* ```$ mvn package``` 

* ```$ docker build -t --image name-- .``` 

* ```$ docker run -d -p 80:80  --name --container name-- --image name-- ``` 